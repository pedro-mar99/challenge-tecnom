
export const ROUTES_PATHS = {
    POST_VENTA: {
        DEFAULT: 'postVenta',
        CALIFICAR: 'calificarServicio',
        FEEDBACK: 'feedBack'
    }
}

export const INTERNAL_PATHS = {
    POST_VENTA_DEFAULT: `${ROUTES_PATHS.POST_VENTA.DEFAULT}`,
    POST_VENTA_CALIFICAR: `${ROUTES_PATHS.POST_VENTA.CALIFICAR}`,
    POST_VENTA_FEEDBACK: `${ROUTES_PATHS.POST_VENTA.FEEDBACK}`,
}

export const INTERNAL_ROUTES = {
    //POST-VENTA
    PV_CALIFICAR_SERVICIO:`/${INTERNAL_PATHS.POST_VENTA_DEFAULT}/${INTERNAL_PATHS.POST_VENTA_CALIFICAR}`,
    PV_FEEDBACK:`/${INTERNAL_PATHS.POST_VENTA_DEFAULT}/${INTERNAL_PATHS.POST_VENTA_FEEDBACK}`,
}